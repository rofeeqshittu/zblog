<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1><%= title %></h1>
    
    <a href="/" class="btn">Go Back</a>
    <br><br>
    
    <div class="edit-form">
        <form action="/edit/<%= post.id %>" method="POST">
            <div class="form-group">
                <label for="ptitle">Title: </label>
                <input type="text" name="ptitle" id="ptitle" value="<%= post.ptitle %>" required>
            </div>
            
            <div class="form-group">
                <label for="pbody">Content: </label>
                <textarea name="pbody" id="pbody" rows="10" cols="50" required><%= post.pbody %></textarea>
            </div>
            
            <button type="submit" class="btn">Save Changes</button>
        </form>
    </div>
</body>
</html>